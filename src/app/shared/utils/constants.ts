import { Position } from '../shared.model';

export const ToastLifeTime = 3000;
export const REVERSED_POSITIONS: { [key: string]: Position } = {
  ar: 'left',
  en: 'right',
};
export const POSITIONS: { [key: string]: Position } = {
  ar: 'right',
  en: 'left',
};
