import { InputBetterValidationDirective } from './input-better-validation.directive';

describe('InputBetterValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBetterValidationDirective();
    expect(directive).toBeTruthy();
  });
});
