import { ClearInputDirective } from './clear-input.directive';

describe('ClearInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearInputDirective();
    expect(directive).toBeTruthy();
  });
});
