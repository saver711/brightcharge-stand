import { Component, OnDestroy, OnInit } from '@angular/core';
import { Operator } from '../../operator.model';
import { ActivatedRoute } from '@angular/router';
import { Subscription } from 'rxjs';

@Component({
  selector: 'bs-operator-wrapper',
  templateUrl: './operator-wrapper.component.html',
  styleUrls: ['./operator-wrapper.component.scss'],
})
export class OperatorWrapperComponent {}
